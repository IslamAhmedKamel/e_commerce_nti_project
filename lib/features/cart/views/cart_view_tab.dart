import 'package:flutter/material.dart';

class CartViewTab extends StatelessWidget {
  const CartViewTab({super.key});

  @override
  Widget build(BuildContext context) {
    return SafeArea(child: Center(child: Text("Cart View")));
  }
}
